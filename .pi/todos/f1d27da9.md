{
  "id": "f1d27da9",
  "title": "[P2] registry_cvm.node_name may be empty — broken REGISTRY_URL passed to new runner",
  "tags": [
    "pr-3",
    "bug",
    "p2",
    "phala"
  ],
  "status": "done",
  "created_at": "2026-02-16T10:54:30.655Z"
}

**File:** `model_orchestrator/infrastructure/phala/_cluster.py`

Added early guard: if registry CVM's `node_name` is empty (e.g. discovered via fallback URLs), raise `PhalaClusterError` instead of constructing a broken `REGISTRY_URL` for the new runner.
