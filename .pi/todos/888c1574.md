{
  "id": "888c1574",
  "title": "[P2] Use raw_decode for JSON parsing of phala CLI output",
  "tags": [
    "pr-3",
    "robustness",
    "p2",
    "phala"
  ],
  "status": "done",
  "created_at": "2026-02-16T10:16:48.389Z"
}

**File:** `model_orchestrator/infrastructure/phala/_cluster.py`, line 522

Changed `json.loads(stdout[json_start:])` → `json.JSONDecoder().raw_decode(stdout[json_start:])` so trailing text after the JSON object (e.g. `{"app_id":"x"}\nDone!`) is handled correctly instead of raising a JSONDecodeError.
